<input type="checkbox" name="done" <%= isDone ? "checked" : "" %>>

<%= task.escape("description") %> <br>

Assigned to:
<% assignments.each(function (assignment) { %>
  <%= assignment.escape("username") %>
<% }) %>
<br>

<% if task.get("deadline") { %>
  Due on:
  <%= task.escape("deadline") %>
<% } %>
